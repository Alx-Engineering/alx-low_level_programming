#include "main.h"
#include <stdio.h>

/**
 * main - Check the code
 *
 * Return: Always 0.
 */
int main(void)
{
char *s = "-123456789";
int number;

number = _atoi(s);
printf("Number: %d\n", number);
return (0);
}
