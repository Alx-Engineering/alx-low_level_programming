#include "main.h"

/**
 * main - Check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    char *str;

    str = "02468"; // Mettez le texte correspondant à la sortie attendue
    puts2(str);
    return (0);
}
