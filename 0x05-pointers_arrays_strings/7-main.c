#include "main.h"

/**
 * main - Check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    char *str;

    str = "56789"; // Mettez le texte correspondant à la sortie attendue
    puts_half(str);
    return (0);
}
