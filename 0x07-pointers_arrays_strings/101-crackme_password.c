#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    char password[12] = "Your-Password"; // Remplacez par le mot de passe

    printf("%s", password);
    return (0);
}
